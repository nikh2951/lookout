// Manual image adjustment implementation belongs here.
// Move the current adjustment state and adjustment application logic here.
// Future versions should attach adjustment state to individual document layers.
export function applyAdjustments(target, adjustments) {
    return { target, adjustments };
}
