// LookOut application entry point.
// Keep global application startup/navigation here.

import { initManualEdit } from "./manual-edit.js";

window.LookOut = window.LookOut || {};
window.LookOut.initManualEdit = initManualEdit;
